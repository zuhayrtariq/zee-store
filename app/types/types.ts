export type ProductDetails = {
    // id: string;
    slug: string;
    name: string;
    description: string;
    price: number;
    // images: { url: string }[]; // Example for product images
    // Add other fields as needed
};